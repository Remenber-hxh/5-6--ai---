# 智巡 · 开发日记 2026-07-23

> 主线:**产品化转向**。从"一个能演示的系统"走向"能卖给多个客户的产品"。
> 当日交付:运维兜底(自动备份)→ 生产部署验收 → 多租户后端地基(Phase 0)全部落地。

---

## 一、当日全景

| 时段 | 主题 | 结果 |
|---|---|---|
| 凌晨 | ICP 备案号落地 + 移动端视口三处硬伤 | 已部署 |
| 上午 | 安全现状审计(对外口径梳理) | 输出结论,无代码 |
| 下午 | 自动备份 + 恢复演练脚本 | 生产实跑通过 |
| 下午 | 13 个积压提交上生产 | 零数据丢失,HTTPS 确认 |
| 傍晚 | 多租户方案评审 + Phase 0 实施 | 5 步全完成 |

**代码量**:未推送 8 个提交,18 个文件,+1289 / -205 行。
**测试**:全量 31 个用例通过,其中租户隔离/边界相关新增 7 条。

---

## 二、运维兜底:从"零备份"到"可备可还原"

### 问题
线上跑了两个月,**一份备份都没有**。文档里只有几行手敲的 `mysqldump` 命令。
数据库 558 条巡检记录、34 个资产、1.2G 现场照片——**对监管场景而言,照片就是留痕证据,丢了等于证据灭失**。

### 交付
- **`scripts/backup.sh`** — 数据库 / 巡检照片 / 密钥三件套一条命令
  - `mysqldump --single-transaction`:InnoDB 热备**不锁表**,业务无感,不用停服务
  - **备份前查磁盘余量**,不够直接中止 —— 宁可这次不备份,也不能写满盘把服务拖挂
  - `flock` 独占:定时任务与手工执行撞车时后来者退出,不产生半份备份
  - 密钥单独打包 `chmod 600`,不与数据混在一个文件
  - 生成 `SHA256SUMS`,恢复前先校验
  - 失败推企业微信群机器人
- **`scripts/restore.sh`** — 恢复 + 演练
  - **默认恢复到临时库,不碰生产**;覆盖生产必须 `--force-production` 且手工输入确认短语
  - 恢复后打印各表条数供核对

### 设计原则
> **备份脚本谁都能跑,恢复脚本必须难跑。**

### 验证(生产实跑)
```
1/3 数据库导出完成(456K)
2/3 照片打包完成(1.2G)
3/3 密钥打包完成
备份完成:合计 1.2G,磁盘剩余 17280MB
```
随即做**恢复演练**到临时库,条数与线上一致:
记录 558 / 资产 34 / 用户 10 / 任务 38 / 审批 13 / 日志 445。

> **没恢复成功过的备份不算备份。** 这一步把"我们有备份"从口号变成事实。

### 一个自己埋的坑
备份默认目录在仓库内 `backups/`,而三个服务的 Docker 构建上下文都是仓库根、部署走 `up -d --build`。
不排除的话 **1.2G 备份会被当构建上下文塞给 Docker**。修:`.dockerignore` / `.gitignore` 各加一行(照 `storage/` 现成范式)。

---

## 三、生产部署:13 个积压提交一次上线

### 风险点
这批提交里包含**版本化迁移系统本身** —— 即生产库**从未跑过迁移**,新后端启动会把 1~7 号迁移全部对着真实生产数据跑一遍。

### 结果:全绿
- `schema_migrations` 1~7 全部记账
- 数据零丢失(日志 445→446,唯一增量是本次登录记录)
- MySQL 容器"Up 8 days"未重建 —— 数据库全程在线
- **HTTPS 确认开启**:`curl -sI https://jadeast.cloud/health` → `HTTP/2 200`;HTTP 被 301 强制跳转

> 上午安全审计里最大的问号(HTTPS 到底开没开)在此闭环:**已开启且稳定运行 8 天**。

### 顺序为什么这么排
**先备份 → 再演练 → 后部署。** 备份脚本比业务变更先落地,部署才有回退底。

---

## 四、多租户改造(Phase 0)—— 当日主体工程

### 4.1 为什么做

产品化三个驱动全部确认:**持续加功能 / 多用户 / 按客户配置**。
其中"按客户配置"经确认是**真多租户**:一套部署、多客户公司共用、数据互相隔离。

**关键判断**:多租户主要是**后端数据模型工程**,必须先于移动端重构做 —— 否则移动端建完才发现 API 没有租户概念,要返工。

### 4.2 架构决策

| 议题 | 决策 | 理由 |
|---|---|---|
| 隔离策略 | 共享 schema + `tenant_id` **行级** | 规模小(34 资产/558 记录/单机);后端已有 Store 抽象,可一处收口 |
| 租户来源 | 从**登录账号**自动带出 | 用户不手选公司:少操作、不泄露客户名单 |
| 收口点 | **Store 层**统一过滤 | 把"忘加过滤导致串户"的风险摁在一个地方 |
| 角色范围 | 内置角色全局,自定义角色归租户 | 客户 A 的自定义角色不该出现在客户 B |
| 超管标记 | users 加 `is_platform_admin` **独立标志位** | "能否跨租户"与"数据归属哪家"正交;用特殊 tenant_id 会让超管自身数据无家可归 |

方案已成文:`inspectai/docs/tenant-and-auth-design.md`(评审通过后实施)。

### 4.3 实施五步

```
[✓] 1  tenants 表 + 默认租户「璟邑科技」          migration 008
[✓] 2  14 张业务表加 tenant_id + 存量回填         migration 009
[✓] 3  User 带 TenantID + 请求级租户解析
[✓] 4  Store 层隔离(资产域 + 记录域 试点)
[✓] 5  两级管理员(平台超管 / 租户管理员)         migration 010
```

**每步都小而独立可验**,与生产已验证的迁移器同一套路(纯新增、幂等、失败即止)。

#### 步骤 1–2:数据库地基

`ADD COLUMN tenant_id NOT NULL DEFAULT 'tenant_default'` 一句话办两件事:
1. 加列时数据库**自动把存量行回填**成默认租户(不需要额外 UPDATE)
2. 代码尚未串好租户的**过渡期**,任何漏设 `tenant_id` 的新行也落到默认租户 = 维持单租户正确行为

`roles` / `role_permissions` **刻意排除**:内置角色要全局共享,语义与"全部回填默认租户"不同,单独处理。

#### 步骤 3:租户随会话流动

用户加载是**一处中心化**的(`userSelectSQL` + `scanUserWithHash`),只改这一处,所有取用户的地方就都带上租户。
新增 `tenantForRequest(r)`,与既有 `userRole` / `currentUserName` **同一按需解析风格**,不动中间件、风险最低。

#### 步骤 4:Store 层隔离(核心)

采用**签名传租户**而非"handler 记得校验":

```go
ListAssets(tenantID)            GetAsset(tenantID, id)
ListRecords(tenantID, limit)    GetRecord(tenantID, id)
```

> **好处:任何漏改的调用点都编译不过。** 整个改造就是靠编译器一处处报错,把 60+ 调用点改全,不可能出现"漏一处静默泄露"。

跨租户访问一律返回 `sql.ErrNoRows` → 上层 404 → **等同不存在**(不泄露存在性)。

#### 步骤 5:两级管理员

| 角色 | 能力 | 边界 |
|---|---|---|
| **平台超管**(璟邑) | 建/停客户租户、跨租户运维 | 唯一能跨租户 |
| **租户管理员**(客户 admin) | 管本租户用户/部门/角色/资产 | 作用域锁死本租户 |

新增 `guardPlatformAdmin` 守卫档位 + `GET/POST /api/tenants`。

---

## 五、自查抓到的三个真问题

**这一节是当日最有价值的部分。** 三个都属于"以为挡住了,其实没挡住"。

### 问题一:跨租户**写**入洞(资产域)

我曾判断"mutation 被 handler 的前置 `GetAsset` 挡住"。审查发现 **`handlePatchAsset` 从权限校验直接调 `UpdateAssetMeta`,中间没有任何租户校验**——租户 A 只要有 `asset_manage` 权限且知道资产 ID,就能改租户 B 的资产。

**修法**:回到设计文档原则,三个 mutation(改属性/换封面/删除)全部在 **Store 层**按租户过滤。
**教训**:**"靠调用方记得检查"的设计一定会漏** —— 它已经被一个 handler 打破。

### 问题二:`OR` 短路使租户过滤失效(记录域)

按归属查记录原本是 `WHERE (归属A) OR (归属B)`。租户条件若直接接在后面会被 `OR` 短路。
**修法**:写成 `WHERE tenant_id=? AND (A OR B)`,并加测试专门验证跨租户按归属查返回空。

### 问题三:建号不继承创建者租户

`handleCreateUser` 未设 `TenantID`,一律回落默认租户 → **租户 B 的管理员建的账号会落到租户 A**。
**修法**:继承创建者所在租户。
**顺带确认**:建号请求体结构里根本没有 `isPlatformAdmin` 字段,租户管理员无法自助提权;并写测试**故意塞 `isPlatformAdmin:true` 与别家 `tenantId`**,验证两者都被忽略。

---

## 六、测试资产

新增 7 条租户隔离/边界用例(真 SQLite,验证 SQL 层 WHERE 实际生效):

| 用例 | 守住什么 |
|---|---|
| `TestMigrationTenants` | 008 建表/种子/记账/重开幂等 |
| `TestMigrationTenantColumns` | 14 表加列、roles 未加、漏设租户落默认 |
| `TestTenantForRequest` | 不同用户解析出各自租户,无会话回落默认 |
| `TestAssetTenantIsolation` | 资产**读**:List 只见本租户、跨租户 Get 不可见 |
| `TestAssetTenantIsolationOnMutations` | 资产**写**:跨租户改名/换封面/删除全部失败 |
| `TestRecordTenantIsolation` | 记录读写 + 冒充租户号改别家记录失败 |
| `TestPlatformAdminBoundary` | 租户管理员 403 / 超管 200 / 短码唯一 |
| `TestCreateUserInheritsTenantAndCannotSelfElevate` | 建号继承租户 + 无法自助提权 |

全量 31 个用例通过。

---

## 七、当前状态与下一步

### 已完成
- Phase 0(后端多租户地基)**全部完成**
- 单客户行为**完全不变** —— 所有数据在默认租户,现网无感

### 未完成 / 已知边界(均有 TODO 标注)
- 其余 13 个业务域的租户隔离(审批、工程任务、提示词等)—— 按"临上第二客户前补齐"的节奏
- `UpsertAsset` 的租户暂由列默认值落定,提交流携带真实租户时需显式串入
- 管理 AI 洞察层为下游消费者,暂按默认租户
- 审批应用流无请求上下文处暂按默认租户

> 这些在单客户下**行为完全正确**,是有意识的分层,不是遗漏。

### 下一步
**Phase 1**:移动端 React + antd-mobile 脚手架与应用外壳(与 admin-web 统一栈,共享 API 层/类型/鉴权 store)。

### 待办
本地 **8 个提交未推送**。Phase 0 是完整里程碑,适合作为一次存档推送。

---

## 八、方法论沉淀

1. **小步迁移**:每个 migration 只做一件事,与生产验证过的迁移器同套路,一步一验。
2. **让编译器当安全网**:改签名而非加检查 —— 漏改会编译失败,而不是静默泄露。
3. **隔离必须在 Store 层收口**:依赖调用方记得校验的设计,一定会被某个 handler 打破。
4. **读写都要测**:只测读会漏掉"知道 ID 就能改别家数据"的写入洞。
5. **提交前自查**:资产域的洞是提交后发现的,此后每步改为提交前审查,记录域与两级管理员的问题都在提交前抓到。
6. **诚实标注边界**:做不到的地方留 TODO 并写清为何当前正确,不假装完整。
